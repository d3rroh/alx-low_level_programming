#include "main.h"
/**
* @n: int to compute as absolute
* _abs - returns if its lower or not
* Return: returns zero according to answer
*/


int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
